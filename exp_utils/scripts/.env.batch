# 1. Define specific (PP, TP) pairs
# Format: ("1,2" "2,1" ...) ("1,8" "2,4")
PP_TP_PAIRS=("1,8" "2,4") 

# 2. Other parameters can still be grids or single values
TARGET_MODELS=("Llama-3.3-70B-Instruct" )                    #  choice: Qwen3-8B, Qwen3-32B, Llama-3.1-8B-Instruct, Llama-3.3-70B-Instruct, vicuna-7b-v1.3,
SPECPIPE_OPTS=(True False)                    #  choice: True, False
PROMPT_COUNTS=(16)                    #  choice: int
# SpecBench task list (for normal_execute_batch_by_spec_task.sh): total, writing, roleplay, reasoning, math, coding, extraction, stem, humanities, translation, summarization, qa, math_reasoning, rag, multiturn
SPECBENCH_TASKS=("total")                  

# Fix Random Seed
SEED="42"                             #  choice: int

# Static parameters
SPPP_VER="1.5"
DROP_RATIO="0.15"                   #  0.3 for Qwen3-32B 
TEMPERATURE="0.0"
COMPILE_CAPTURE="True"
FORCE_GEN_STEP="False"
GEN_STEP="150" 
THROUGHPUT_ANALYZE="True"
DATASET="specbench"
TOKEN_BUDGET="2048"
MAX_SEQS="1024"
FIX_BATCH="False"